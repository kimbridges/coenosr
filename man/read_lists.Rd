% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_lists.R
\name{read_lists}
\alias{read_lists}
\title{Read a relevé file in "site, then its species" (ragged) form}
\usage{
read_lists(path, present = "1", encoding = "latin1")
}
\arguments{
\item{path}{Path to the data file.}

\item{present}{Cover value assigned to a species given without a cover.
Defaults to \code{"1"}, matching the presence marker of a 1/0 wide table.}

\item{encoding}{File encoding passed to \code{\link[=readLines]{readLines()}}.}
}
\value{
A \code{coenos_rel} table (see \code{\link[=read_rel]{read_rel()}}): columns \code{releve}, \code{species},
\code{cover}, ready for \code{\link[=constancy]{constancy()}}, \code{\link[=classify_species]{classify_species()}}, and the rest of the
workflow.
}
\description{
Read vegetation (or any site-by-item) data recorded as one line per site: a
site code followed by the species found there. Lines need not be the same
length — each site lists only what was present — which makes this the natural
form for field recording (for example, dictating species into a recorder and
transcribing). It is a close, modern cousin of the COENOS \code{.REL} format read
by \code{\link[=read_rel]{read_rel()}}.
}
\details{
Each non-blank line is split on commas. The first field is the \strong{site
(relevé) code}; the remaining fields are species. Cover values are
\strong{detected automatically}: within a species field, a trailing token that
looks like a cover value — a Braun-Blanquet symbol (\code{+}, \code{R}) or a single
digit — is taken as the cover, and the rest of the field is the species name.
A field with no such trailing token is a species marked present (with the
\code{present} value as its cover). So both of these read correctly, with and
without cover:

\if{html}{\out{<div class="sourceCode">}}\preformatted{1, G, L, M, C            # presence only
1, G 4, L 2, M +, C 3    # with Braun-Blanquet covers
}\if{html}{\out{</div>}}

Multi-word species names are supported (\verb{Carex lyngbyei 5} reads as the
species \code{"Carex lyngbyei"} with cover \code{"5"}). Lines containing \code{&&} are
treated as comments and ignored. If a species is listed twice for one site,
the last cover wins. Unlike \code{\link[=read_rel]{read_rel()}}, the full site code is kept (it is
not truncated to six characters).
}
\examples{
rel <- read_lists(system.file("extdata", "simple_10x15_simplified.txt",
                              package = "coenosr"))
nlevels(rel$releve)   # 10 sites
nlevels(rel$species)  # 15 species

}
\seealso{
\code{\link[=read_2way]{read_2way()}} for the wide-matrix form, \code{\link[=read_rel]{read_rel()}} for COENOS files.
}
