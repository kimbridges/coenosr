% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential_table.R
\name{differential_table}
\alias{differential_table}
\title{Extract the differential table for display}
\usage{
differential_table(x, include = c("core", "full"))
}
\arguments{
\item{x}{A \code{coenos_table} from \code{\link[=coenos_table]{coenos_table()}}, or a \code{coenos_groups} object
from \code{\link[=form_groups]{form_groups()}} (which is arranged first).}

\item{include}{\code{"core"} (default) for the grouped species and their relevés,
or \code{"full"} for the whole sorted table.}
}
\value{
A data frame with columns \code{group}, \code{species}, and one column per
relevé, in sorted order.
}
\description{
Pulls the display-ready differential table out of a sorted \code{\link[=coenos_table]{coenos_table()}}:
the grouped species, the relevés they characterise, and a group label for
each species. This is the \emph{preparation} step behind a printed Braun-Blanquet
table -- it does the row and column selection so you do not have to, and
returns a plain data frame you can render with any table tool.
}
\details{
By default (\code{include = "core"}) the result holds only the species that fell
into a differential group and only the relevés that belong to at least one
group -- the differential core, the part of the table that carries the
pattern. With \code{include = "full"} every species and relevé is returned in
sorted order, the ungrouped species labelled \code{"companion"} or \code{"rare"}.

Columns are a \code{group} label, the \code{species}, and then the relevés in sorted
order. Absent cells are empty strings. The relevé columns keep their full
names; see \code{\link[=coenos_gt]{coenos_gt()}} to render them with shortened headers.
}
\examples{
rel <- read_rel(system.file("extdata", "DIEREN.REL", package = "coenosr"))
differential_table(form_groups(rel))

}
\seealso{
\code{\link[=coenos_table]{coenos_table()}}, \code{\link[=coenos_gt]{coenos_gt()}}
}
