% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_rel.R
\name{read_rel}
\alias{read_rel}
\title{Read a COENOS releve file (\code{.REL})}
\usage{
read_rel(path, encoding = "latin1")
}
\arguments{
\item{path}{Path to a \code{.REL} file.}

\item{encoding}{File encoding passed to \code{\link[=readLines]{readLines()}}. Defaults to
\code{"latin1"}; the bundled example data uses single-byte characters.}
}
\value{
A data frame (with S3 class \code{"coenos_rel"}) carrying one row per
releve-by-species occurrence, with columns:
\describe{
\item{\code{releve}}{releve code, a factor in file order.}
\item{\code{species}}{8-character COENOS species key, a factor in
first-appearance order.}
\item{\code{cover}}{cover estimate, a single character.}
}
}
\description{
Parse a COENOS-format releve file into a tidy long table of
releve / species / cover records. The format is the ASCII input read by the
1991 COENOS program: each releve begins with a code line, followed by one
species per line (four-character genus, a space, four-character species
epithet, then a one-character cover estimate), and ends with a \code{$} on its
own line.
}
\details{
Parsing follows the rules documented in the original COENOS help file:
\itemize{
\item Only the \strong{six rightmost characters} of a releve code are kept
(codes may be longer; only the tail is significant).
\item A species is identified by an 8-character key formed from the genus
(columns 1--4) and the epithet (columns 6--9). The key is treated as an
opaque token, so non-standard entries (a trailing-digit epithet such as
\code{"380 "}, an \code{x} in a genus, a \verb{?} in an epithet) are preserved verbatim
rather than rejected.
\item The cover estimate is the first non-blank character after the epithet
field. A line with \strong{no} cover estimate is ignored (this is how the
releve code line and stray text are skipped within a releve).
\item If a species is listed more than once in one releve, the \strong{last}
cover value is kept.
\item Lines containing \code{&&} are comments and are ignored; blank lines are
skipped.
\item Reading is case-sensitive (\verb{CARE AQUA} and \verb{CARE aqua} differ).
}
Cover values are returned verbatim as single characters, so both the
Braun-Blanquet symbolic scale (\verb{5 4 3 2 1 + R}) and numeric cover scales are
supported. Cover is recorded for display and is not interpreted here.
}
\examples{
rel <- read_rel(system.file("extdata", "DIEREN.REL", package = "coenosr"))
nlevels(rel$releve)   # 18 releves
nlevels(rel$species)  # 116 species
head(rel)

}
\seealso{
\code{\link[=constancy]{constancy()}} for per-species occurrence counts.
}
