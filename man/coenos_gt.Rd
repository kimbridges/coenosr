% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential_table.R
\name{coenos_gt}
\alias{coenos_gt}
\title{Render a differential table with gt}
\usage{
coenos_gt(
  x,
  include = c("core", "full"),
  shorten = TRUE,
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{x}{A \code{coenos_table} from \code{\link[=coenos_table]{coenos_table()}}, or a \code{coenos_groups} object
from \code{\link[=form_groups]{form_groups()}}.}

\item{include}{Passed to \code{\link[=differential_table]{differential_table()}}: \code{"core"} (default) or
\code{"full"}.}

\item{shorten}{Logical; shorten the relevé headers for display. Default
\code{TRUE}.}

\item{title, subtitle}{Optional table header text. \code{NULL} for none.}
}
\value{
A \code{gt_tbl} object.
}
\description{
A one-call convenience that turns a sorted \code{\link[=coenos_table]{coenos_table()}} into a formatted
\pkg{gt} table -- the differential species groups across their relevés, with
the groups as labelled row sections. It is a thin wrapper over
\code{\link[=differential_table]{differential_table()}} for people who want the publication-ready table
without writing the rendering code.
}
\details{
Requires the \pkg{gt} package (in Suggests). Relevé headers are shortened by
default -- a leading non-numeric prefix and its zeros are dropped, so
\code{"BILL04"} shows as \code{"4"} -- unless that would make two headers collide, in
which case the full names are kept.
}
\examples{
\dontrun{
rel <- read_rel(system.file("extdata", "DIEREN.REL", package = "coenosr"))
coenos_gt(form_groups(rel))
}

}
\seealso{
\code{\link[=differential_table]{differential_table()}}, \code{\link[=coenos_table]{coenos_table()}}
}
