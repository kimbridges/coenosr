% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{classify_species}
\alias{classify_species}
\title{Classify species by constancy for COENOS group formation}
\usage{
classify_species(rel)
}
\arguments{
\item{rel}{A \code{coenos_rel} table from \code{\link[=read_rel]{read_rel()}}.}
}
\value{
A data frame with columns \code{species}, \code{constancy}, and \code{status} (a
factor with levels \code{eligible}, \code{ubiquitous}, \code{rare}), in first-appearance
order. The number of relevés and the ceiling are attached as attributes
\code{n_releve} and \code{ceiling}.
}
\description{
COENOS divides species into three constancy classes before it forms
differential groups. This function reproduces that split, which is the
trimming step preceding group formation.
}
\details{
Let \code{n} be the number of relevés. A species' constancy is the number of
relevés in which it occurs (see \code{\link[=constancy]{constancy()}}). The classes are:
\describe{
\item{\code{rare}}{constancy \verb{< 3} -- too infrequent to define a group. These
are the species COENOS lists in the "low constancy" table footnote.}
\item{\code{ubiquitous}}{constancy \verb{>= floor(2 * n / 3)} -- too widespread to be
diagnostic. These become high-constancy companions, excluded from group
formation.}
\item{\code{eligible}}{everything in between -- the candidates from which
differential species groups are formed. An eligible species that does
not end up in any group is reported as an ordinary companion.}
}
The \code{floor(2 * n / 3)} ceiling is the integer form of the help file's
"constancy higher than 66\%". It was recovered exactly from the COENOS
restart files for three datasets (n = 18, 25, 63); the integer floor, not the
percentage, is what the program uses (for n = 25 it excludes a species
present in 16 of 25 relevés, i.e. 64\%).
}
\examples{
rel <- read_rel(system.file("extdata", "DIEREN.REL", package = "coenosr"))
cl <- classify_species(rel)
table(cl$status)
attr(cl, "ceiling")  # floor(2 * 18 / 3) = 12

}
\seealso{
\code{\link[=constancy]{constancy()}}, \code{\link[=read_rel]{read_rel()}}
}
