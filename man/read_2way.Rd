% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_2way.R
\name{read_2way}
\alias{read_2way}
\title{Read a wide two-way table (a species-by-sites matrix)}
\usage{
read_2way(
  path,
  absent = c("0", "", "NA"),
  transpose = FALSE,
  encoding = "latin1",
  ...
)
}
\arguments{
\item{path}{Path to a CSV-style file.}

\item{absent}{Character vector of cell values that mean "absent" and are
dropped. Defaults to \code{c("0", "", "NA")}. \code{NA} cells are always dropped.}

\item{transpose}{Logical; if \code{TRUE}, rows are sites and columns are species.
Defaults to \code{FALSE} (rows are species, columns are sites — the COENOS table
orientation).}

\item{encoding}{File encoding passed to \code{\link[utils:read.table]{utils::read.csv()}}.}

\item{...}{Further arguments passed to \code{\link[utils:read.table]{utils::read.csv()}}.}
}
\value{
A \code{coenos_rel} table (see \code{\link[=read_rel]{read_rel()}}), identical in form to what the
other readers produce.
}
\description{
Read data held as a rectangular two-way table: species down the rows, sites
across the columns, and a value in each cell. This is the familiar
spreadsheet form, the way many people store co-occurrence data. The cell may
be a cover value or a simple presence/absence flag (\code{1}/\code{0}).
}
\details{
The file is read with \code{\link[utils:read.table]{utils::read.csv()}}. The \strong{first column} holds the row
identifiers (species, by default) and its header is ignored; the remaining
\strong{column names} are the site identifiers. Each cell that is not an "absent"
marker becomes one occurrence, carrying the cell's value as its cover. By
default a cell counts as absent if it is \code{0}, blank, or \code{NA}, so a 1/0 matrix
and a cover matrix (covers in the filled cells, blanks elsewhere) both read
correctly.

Set \code{transpose = TRUE} if your table is the other way round — sites down the
rows and species across the columns.
}
\examples{
rel <- read_2way(system.file("extdata", "simple_10x15_input.txt",
                             package = "coenosr"))
nlevels(rel$releve)   # 10 sites
nlevels(rel$species)  # 15 species

}
\seealso{
\code{\link[=read_lists]{read_lists()}} for the ragged "site, then its species" form,
\code{\link[=read_rel]{read_rel()}} for COENOS files.
}
